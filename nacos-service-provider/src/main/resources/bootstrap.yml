spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:8848
#    sentinel:
#      transport:
#        port: 8179
#        dashboard: localhost:8080
  application:
    name: nacos-service-provider